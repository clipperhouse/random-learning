 {{#videos}}
<a href="{{url}}">
    <h3>
        {{title}}
    </h3>
    <p>
        <img src={{image_url}} alt="{{title}}" />
    </p>

    <p class="{{site_class}}">
        {{site}}, {{duration}}
    </p>
</a>
<hr /> {{/videos}}